alert ('Leia com atenção ;)')

var darkMode = false; 

function whiteMode (){
    if (darkMode === false)
        $("p").css("color,", "white"); 
        darkmode == true; 
    }

function confirmar()
{
    window.alert('Leia com atenção')
}